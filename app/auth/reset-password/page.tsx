"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { getSupabaseBrowserClient } from "../../../lib/supabase";
export default function ResetPasswordPage() { const client = getSupabaseBrowserClient(); const router = useRouter(); const [password, setPassword] = useState(""); const [message, setMessage] = useState(""); const submit = async (event: React.FormEvent) => { event.preventDefault(); if (!client) return; const { error } = await client.auth.updateUser({ password }); if (error) setMessage(error.message); else { setMessage("비밀번호가 변경됐어요."); setTimeout(() => router.push("/"), 900); } }; return <main className="grid min-h-screen place-items-center bg-canvas px-5"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl border border-line bg-white p-7 shadow-card"><h1 className="text-xl font-bold">새 비밀번호 설정</h1><p className="mt-2 text-xs text-muted">새 비밀번호를 입력해 주세요.</p><input required minLength={6} type="password" value={password} onChange={event => setPassword(event.target.value)} className="mt-6 w-full rounded-lg border border-line px-3 py-2.5 text-sm outline-none focus:border-coral" placeholder="새 비밀번호 6자 이상" /><button className="mt-3 w-full rounded-lg bg-coral py-3 text-sm font-bold text-white">비밀번호 변경</button>{message && <p className="mt-4 text-xs text-coral-ink">{message}</p>}</form></main>; }
