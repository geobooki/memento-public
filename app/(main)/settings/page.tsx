"use client";
import { useState } from "react"; import { Header } from "../../../components/record-widgets"; import { useRecords } from "../../../components/records-provider";
export default function SettingsPage() { const { categories, addCategory, signOut } = useRecords(); const [label, setLabel] = useState(""); const submit = async () => { if (label.trim()) { await addCategory(label.trim()); setLabel(""); } }; return <><Header title="설정" onSearch={() => {}} /><section className="max-w-xl rounded-xl border border-line bg-white p-5"><h2 className="text-base font-bold">카테고리 관리</h2><p className="mt-2 text-xs leading-6 text-muted">기본 카테고리 외에 나만의 분류를 추가할 수 있어요.</p><div className="mt-5 flex gap-2"><input value={label} onChange={event => setLabel(event.target.value)} onKeyDown={event => { if (event.key === "Enter") submit(); }} placeholder="새 카테고리 이름" className="w-full rounded-lg border border-line px-3 py-2 text-sm outline-none focus:border-coral" /><button onClick={submit} className="rounded-lg bg-coral px-4 py-2 text-xs font-bold text-white">추가</button></div><div className="mt-5 flex flex-wrap gap-2">{categories.map(item => <span key={item.key} className={`rounded px-2 py-1 text-xs ${item.color}`}>{item.label}</span>)}</div></section><button onClick={signOut} className="mt-5 rounded-lg border border-line bg-white px-4 py-2 text-xs text-muted">로그아웃</button></>; }
